name: decoder
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  shared_preferences: ^2.2.0
  intl: ^0.20.2  # Added for date formatting in profiles
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  url_launcher: ^6.2.4
  in_app_purchase: ^3.1.13
  flutter_dotenv: ^6.0.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
      - .env 
      - assets/