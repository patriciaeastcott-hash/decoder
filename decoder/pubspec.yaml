name: decoder
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Icons
  cupertino_icons: ^1.0.8

  # Networking
  http: ^1.2.0

  # Local Storage
  shared_preferences: ^2.2.2

  # Opening Links (Privacy Policy)
  url_launcher: ^6.2.4

  # In-App Purchases (Compliance)
  in_app_purchase: ^3.1.13

  flutter_dotenv: ^5.1.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
      - .env 